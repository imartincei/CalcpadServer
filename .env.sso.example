# Example environment configuration for SSO authentication
# Copy this file to .env and modify the values for your SSO provider

# Authentication Provider (Local, OIDC, SAML)
Authentication__Provider=OIDC

# OIDC Configuration
Authentication__OIDC__Enabled=true
Authentication__OIDC__Authority=https://your-identity-provider.com
Authentication__OIDC__ClientId=calcpad-production
Authentication__OIDC__ClientSecret=your-production-client-secret
Authentication__OIDC__Scope=openid profile email
Authentication__OIDC__ResponseType=code
Authentication__OIDC__CallbackPath=/signin-oidc

# Disable local authentication when using SSO
Authentication__Local__Enabled=false

# MinIO Configuration
MinIO__Endpoint=minio:9000
MinIO__AccessKey=calcpad-admin
MinIO__SecretKey=calcpad-password-123
MinIO__UseSSL=false

# Database
ConnectionStrings__DefaultConnection=Data Source=/app/data/calcpad.db