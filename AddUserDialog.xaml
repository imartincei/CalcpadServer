<Window x:Class="CalcpadViewer.AddUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User" Height="350" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- User Form -->
        <GroupBox Header="New User Details" Grid.Row="0">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
                <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="Email:" VerticalAlignment="Center"/>
                <TextBox x:Name="EmailTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
                
                <Label Grid.Row="2" Grid.Column="0" Content="Password:" VerticalAlignment="Center"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="5"/>
                
                <Label Grid.Row="3" Grid.Column="0" Content="Confirm:" VerticalAlignment="Center"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="3" Grid.Column="1" Margin="5"/>
                
                <Label Grid.Row="4" Grid.Column="0" Content="Role:" VerticalAlignment="Center"/>
                <ComboBox x:Name="RoleComboBox" Grid.Row="4" Grid.Column="1" Margin="5">
                    <ComboBoxItem Content="Viewer" Tag="Viewer"/>
                    <ComboBoxItem Content="Contributor" Tag="Contributor" IsSelected="True"/>
                    <ComboBoxItem Content="Admin" Tag="Admin"/>
                </ComboBox>
            </Grid>
        </GroupBox>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CreateButton" Content="Create User" Padding="20,5" Margin="0,0,10,0" 
                    Click="CreateButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="20,5" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>