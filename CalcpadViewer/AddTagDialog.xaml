<Window x:Class="CalcpadViewer.AddTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Tag" Height="200" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Add New Pre-Defined Tag" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <Label Grid.Row="1" Content="Tag Name:" />
        <TextBox x:Name="TagNameTextBox" Grid.Row="2" Margin="0,5,0,15" MaxLength="100"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AddButton" Content="Add" Padding="20,5" Margin="0,0,10,0" 
                    Click="AddButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="20,5" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>