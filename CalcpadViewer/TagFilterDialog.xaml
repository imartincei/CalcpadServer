<Window x:Class="CalcpadViewer.TagFilterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter by Tags" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Search Box -->
        <TextBox x:Name="TagSearchBox" Grid.Row="0" Height="25" Margin="0,0,0,10"
                 TextChanged="TagSearchBox_TextChanged"
                 Background="LightGray" Padding="5,2"
                 Text="Search tags..."/>
        
        <!-- Selected Tags Summary -->
        <TextBlock x:Name="SelectedTagsSummary" Grid.Row="1" 
                   Text="No tags selected" FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <!-- Available Tags List -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,10">
            <ListBox x:Name="AvailableTagsListBox" 
                     ItemsSource="{Binding FilteredTags}"
                     SelectionMode="Multiple"
                     SelectionChanged="AvailableTagsListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" 
                                  IsChecked="{Binding IsSelected}"
                                  Checked="TagCheckBox_Checked"
                                  Unchecked="TagCheckBox_Unchecked"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="SelectAllButton" Content="Select All" 
                    Width="80" Height="30" Margin="0,0,10,0" 
                    Click="SelectAllButton_Click"/>
            <Button x:Name="ClearAllButton" Content="Clear All" 
                    Width="80" Height="30" Margin="0,0,10,0" 
                    Click="ClearAllButton_Click"/>
        </StackPanel>
        
        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" 
                    Width="80" Height="30" Margin="0,0,10,0" 
                    Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" 
                    Width="80" Height="30" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>